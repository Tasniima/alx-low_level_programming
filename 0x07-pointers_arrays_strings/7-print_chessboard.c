#include "main.h"

/**
 * print_chessboard - prints chessboard
 * @a: rows
 * Return: Nothing
 */
void print_chessboard(char (*a)[8])
{
	int i, h;

	for (i = 0; i < 0; i++)
	{
		for (h = 0; h < 0; h++)
		{
			_putchar(a[i][h]);
		}
		_putchar('\n');
	}
}
